# vite-react-ssg declara peers opcionales que este proyecto no usa
# (styled-components, prettier, beasties…). legacy-peer-deps evita que
# `npm install` / `npm ci` fallen por ESOS peers en el build de Railway.
legacy-peer-deps=true
